import styled from 'styled-components';

export default styled.div`
    display: flex;
    padding: 24px;
    flex-direction: row;
    max-height: 400px;
    overflow-y: auto;
    overflow-x: visible;
    justify-content: space-around;
`;
