import styled from 'styled-components';

export default styled.div`
    display: flex;
    flex-direction: row;
    padding-top: 24px;
    max-height: 400px;
    overflow-y: auto;
    overflow-x: visible;
    justify-content: space-around;
`;
